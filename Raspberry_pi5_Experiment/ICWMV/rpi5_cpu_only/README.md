# ICWMV Raspberry Pi 5 CPU

이 디렉토리는 ICWMV 1단계 경량 2-모델 경로를 Raspberry Pi 5 CPU만으로 측정할 때 사용합니다.

## 설치

```bash
pip install -r requirements.txt
```

## 실행

```bash
bash run_benchmark.sh /path/to/test_image.jpg
```

결과는 `results/`, 로그는 `logs/`에 저장됩니다.
