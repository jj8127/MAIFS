# ICWMV Raspberry Pi 5 + PCIe HAT

이 디렉토리는 ICWMV 1단계 경량 2-모델 경로를 Raspberry Pi 5 + PCIe 연결 HAT 환경에서 측정할 때 사용합니다.

## 설치

```bash
bash setup_env.sh
```

## 실행

```bash
bash run_benchmark.sh /path/to/test_image.jpg
```

장치가 탐지되지 않으면 `unmeasured` JSON이 남습니다.
