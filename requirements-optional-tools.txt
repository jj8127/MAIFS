# Optional tool/runtime dependencies for full backend activation.
# Install after requirements.txt:
#   .venv-qwen/bin/python -m pip install -r requirements-optional-tools.txt

# Keep numpy<2 for IMDLBenCo/jpeglib ABI compatibility in this project.
numpy==1.26.4

# Headless OpenCV avoids libGL dependency in server/CI environments.
opencv-python-headless==4.11.0.86

# CAT-Net runtime deps
jpegio==0.2.8
torch-dct==0.1.6

# FatFormer / Mesorch / CAT-Net related optional deps
yacs==0.1.8
pytorch-wavelets==1.3.0
timm==1.0.24
pywavelets==1.8.0
imdlbenco==0.1.44
ftfy==6.3.1
regex==2026.1.15
